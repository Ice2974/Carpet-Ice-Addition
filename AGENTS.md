# AGENTS.md

本文件是给 Codex / Agent 使用的仓库工作规则。它只记录协作方式、修改边界和验证要求，不重复维护项目当前状态、规则清单、版本清单或发布信息。

## 先读项目资料

在开始任何修改前，先查看仓库内的项目资料：

- 在仓库内对应相对路径为：`docs/`
- 规则文档、命令文档、开发说明、发布说明等项目事实，以 `docs/` 和当前源码为准。
- `references/` 是参考资料目录，只在任务明确要求参考第三方源码、外部资料或指定参考实现时查看；没有特别说明时不要主动扫描、引用或修改其中内容。
- 不要把 `AGENTS.md` 当作项目状态清单；这里的信息只用于指导 Agent 如何工作。

如果 `AGENTS.md` 与 `docs/` 或源码冲突，以源码和 `docs/` 中的最新内容为准，并在回复中说明发现的冲突。

## 基本工作原则

- 修改前先阅读相关源码、配置、文档，不要只根据文件名猜测实现。
- 优先做最小必要修改，不做与任务无关的重构、格式化或依赖升级。
- 不要保留历史遗留描述；项目链接、许可证、发布元数据等以仓库当前文件为准。
- 遇到跨版本逻辑时，先确认目标平台的 API、方法签名、Mixin descriptor 和已有 shared 引入方式。
- 不能验证的内容要明确说明，不能伪装为已测试或已确认。
- 对实现范围、跨版本差异、第三方来源、兼容性或验证结果不能确定时，不要自行下结论；在回复的“待人工确认项”中列出需要人工确认的问题和建议确认方式。
- 不要根据 `AGENTS.md` 推断当前支持版本、规则数量或功能状态；这些信息以 `docs/` 和源码为准。

## 目录职责约定

- `common/`：放置规则定义、翻译、公共配置管理、与具体 Minecraft / Yarn / Mojmap 签名差异无关的工具逻辑。
- `versions/shared/`：只作为平台源码复用目录，不是独立 Gradle 子项目，不直接产出 jar。
- `versions/platform-*`：放置平台专属 Mixin、版本桥接、兼容层，以及无法稳定放入 `common` 或 `versions/shared/` 的实现。
- `docs/`：放置规则、命令、开发说明等项目资料。新增或调整规则 / 命令 / 记录器时必须同步检查相关文档。
- `references/`：本地参考资料目录，可能不存在于公开仓库；只在任务明确要求参考第三方源码、外部资料或指定参考实现时允许查看，不作为项目事实来源，不修改其中内容。

## shared 源码规则

- 严禁把整个 `versions/shared/` 加入 `settings.gradle`。
- shared 源码必须由平台模块通过自己的 `sourceSets` 显式引入。
- 只有多个平台已验证完全一致的实现才能迁入 shared。
- 非 Mixin 辅助类迁入 shared 前，要确认对应平台源码完全一致。
- Mixin 迁入 shared 前，要确认目标类、目标方法、注入点和 descriptor 全部一致。
- 若多个 shared 档位存在同名类，只能按必要子目录或必要包路径引入，避免同名源码同时进入同一平台编译集。
- 不要为了减少目录数量强行合并 shared 档位；只要签名或注入点断裂，就保留在平台模块或拆分新 shared 档位。

## 跨版本兼容规则

- 跨小版本差异优先通过平台模块、版本专属 Mixin 或平台适配类处理。
- 不优先使用运行期字符串版本号判断来偷渡兼容。
- 依赖版本不匹配时，优先依赖 `fabric.mod.json` 的 `depends` 在启动阶段 fail-fast，不做运行期静默降级。
- mixin 配置视为必需组件，平台模块的 mixins 配置应保持 `required=true`，除非任务明确要求调整。
- 对运行期非预期异常可以记录日志并阻断当前功能，但不能让玩家或调用者误以为操作已成功。

## 规则修改约定

新增或修改 Carpet 规则时，至少检查：

- 规则定义类
- 翻译提供类
- 中英文语言文件
- 相关 Mixin / Helper / 配置类
- `docs/rules.md`
- `docs/rules_en.md`
- 受影响平台的入口类、mixin json、sourceSets

约定：

- 中文规则名使用 `.name`。
- 中英文规则介绍使用 `.desc`。
- 英文规则名默认直接显示内部名，不额外新增英文 `.name`。
- 规则文档中的规则介绍需要与游戏内规则介绍保持一致。
- 若规则正文包含来源说明，中英文文档需要同步保留或同步调整。
- 中文规则名只写中文显示名，不在后面重复追加内部名。

## 命令修改约定

新增或调整命令时，至少检查：

- 命令注册入口
- 命令实现类
- 规则开关与命令树刷新逻辑
- 权限判断
- 命令反馈文本和错误文本
- 配置持久化逻辑
- `docs/commands.md`
- `docs/commands_en.md`

约定：

- 面向玩家的命令反馈、错误提示、配置显示文本优先写入语言文件，不直接硬编码长文本。
- 如果规则变更会影响命令树，需要监听对应规则并刷新命令树。
- 与版本签名无关的命令业务逻辑优先放在 `common/`。
- 命令源类型、注册表 API、文本组件或实体 API 出现跨版本断裂时，放入 `versions/shared/` 或平台模块。

## 记录器修改约定

当前项目可以没有记录器；后续新增时按本节处理。这里的“记录器 / logger”指 Carpet `/log` 体系下由玩家订阅的游戏内信息输出，不是 SLF4J / Log4j 服务端日志。

新增或调整记录器时，至少检查：

- 扩展入口类的 `registerLoggers()` 接入点
- logger 注册类、显示类或辅助类
- 对应事件触发点、Mixin、Helper 或 HUD 更新入口
- logger 内部名、默认 option、可选 options、是否需要严格校验 option
- 是否属于普通聊天记录器，还是 HUD / Tab 列表类记录器
- 高频路径是否有订阅状态快速判断，避免无人订阅时仍计算昂贵内容
- `docs/loggers.md`
- `docs/loggers_en.md`
- 受影响平台的入口类、mixin json、sourceSets

约定：

- 不要新增独立的 `/log` 命令；优先接入 Carpet 自带 `/log` 订阅体系。
- logger 内部名应稳定、语义明确，并与文档中的名称一致。
- logger 可以依赖某条规则，但不要默认把“规则开关”和“logger 订阅”混为一件事；如果二者存在联动，需要在文档中明确说明。
- 普通事件型输出优先通过 Carpet logger 向已订阅玩家发送；不要绕过订阅系统直接广播给所有玩家。
- HUD / Tab 列表类输出应按目标 Carpet API 的现有 HUD logger / HUDController 模式实现，不要硬编码跨版本不稳定的方法名。
- 高频事件中必须先判断是否存在订阅者或对应加速字段，再组装文本、扫描世界或遍历实体。
- logger 有 option 时，需要明确默认 option 和允许的 options；维度过滤、brief / full、dynamic 等模式需要在中英文记录器文档中写清楚。
- logger 输出文本属于玩家可见文本时，优先复用项目既有文本构造和翻译约定，避免在多个平台复制长文本。
- 跨版本新增 logger 时，先确认目标 Carpet 版本中 `Logger`、`HUDLogger`、`LoggerRegistry`、`HUDController` 等 API 的包名、构造方式和方法签名；出现差异时放入 `versions/shared/` 的合适档位或平台模块。
- 新增记录器文档时，只写当前支持的 logger、用途、订阅方式和 options，不追加无关历史流水账。

## 配置文件约定

- 若功能存在独立运行时配置，优先放在当前世界 / 存档目录下，而不是全局配置目录。
- 配置读取失败时应记录日志并回退默认值。
- 配置写入失败时应记录日志，并向命令执行者返回明确错误。
- 不允许静默吞掉关键异常，导致玩家误以为配置已生效。

## 文档维护约定

- `AGENTS.md` 不维护规则总数、支持版本清单、当前 shared 类清单、依赖版本号或 release notes。
- 项目状态类信息写入 `docs/` 中对应文档。
- 规则文档用于说明规则本身；命令文档用于说明命令用法；记录器文档用于说明记录器功能。
- 新增或调整命令时，同步维护 `docs/commands.md` 与 `docs/commands_en.md`。
- 新增或调整规则时，同步维护 `docs/rules.md` 与 `docs/rules_en.md`。
- 新增或调整记录器时，同步维护 `docs/loggers.md` 与 `docs/loggers_en.md`。
- 对于新增或调整的内容，格式需要与旧内容一致。
- 文档更新时直接维护当前状态，不追加无关历史流水账。

## 版本号修改约定

当任务涉及修改 Mod 版本号时，必须同步检查并更新以下位置：

- `gradle.properties`
  - 以其中的 Mod 版本字段为构建与发布版本号来源。
- `docs/` 目录下的所有文档
  - `docs/` 内各文档可能都在顶部标注了当前 Mod 版本号，修改版本号时必须逐一检查并同步更新。
  - 不要只更新中文文档；英文文档也必须同步检查。
  - 如果 `docs/` 下存在新增文档或子目录文档，也需要一并检查。

约定：

- 版本号修改应保持构建配置、开发说明、规则文档、命令文档、记录器文档和发布说明中的版本标注一致。
- 修改前后应使用全文搜索确认旧版本号是否仍残留在 `gradle.properties` 或 `docs/` 中。
- 如果某些旧版本号属于历史记录、示例、兼容范围或 release notes，不能确定是否应该替换时，不要擅自改写，需在回复的“待人工确认项”中列出。
- 除非任务明确要求，不要因为修改 Mod 版本号而顺手修改 README、许可证、发布平台元数据或无关构建逻辑。

## 构建与验证

- 默认使用仓库内 `./gradlew` 执行编译、构建和验证。
- 依赖版本、平台参数和构建基线以 `gradle.properties` 及当前构建脚本为准，不要从 `AGENTS.md` 读取硬编码版本号。
- 如果 `docs/` 中另有本地 Gradle 或构建环境约定，优先按 `docs/` 执行。
- 涉及 `common/` 改动时，至少验证 `:common:compileJava` 和受影响平台模块的 `compileJava`。
- 涉及跨平台公共逻辑时，优先确认所有当前支持平台都能完成编译。
- 如果因环境限制无法运行验证命令，需要在回复中明确说明未验证的项目和原因。

## 默认不要修改的文件

除非任务明确要求，默认不要修改：

- `README.md`
- `README_en.md`
- `.gitignore`
- `LICENSE`
- `references/` 目录内容
- `AGENTS.md`，除非任务明确要求更新 Agent 工作规则
- 发布平台元数据
- 与当前任务无关的构建脚本

## 回复要求

完成任务后，回复中说明：

- 修改了哪些文件
- 为什么这样改
- 运行了哪些验证命令
- 哪些内容未验证，以及原因
- 待人工确认项；如果没有，就写“无”
- 是否发现与 `docs/` 或现有源码不一致的地方
